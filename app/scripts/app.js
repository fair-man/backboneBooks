define([
  "jquery",
  "underscore",
  "backbone",
  "handlebars",
  "bootstrap"
], function ($, _, Backbone, Handlebars) {
  var app = {};
  console.log("Module 'app.js' ~~~Loaded~~~");
  return app;
});